let Joi,User,RefreshToken,bcrypt,JwtService,CustomErrorHandler,REFRESH_SECRET,refreshToken;_6b8‍.x([["default",()=>_6b8‍.o]]);_6b8‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_6b8‍.w("../../models",[["User",["User"],function(v){User=v}],["RefreshToken",["RefreshToken"],function(v){RefreshToken=v}]]);_6b8‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_6b8‍.w("../../services/JwtService",[["default",["JwtService"],function(v){JwtService=v}]]);_6b8‍.w("../../services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_6b8‍.w("../../config",[["REFRESH_SECRET",["REFRESH_SECRET"],function(v){REFRESH_SECRET=v}]]);_6b8‍.w("../../models/refreshToken",[["default",["refreshToken"],function(v){refreshToken=v}]]);






const registerController = {
    async register(req, res, next) {
    // CHECKLIST
    // [ ] validate the request
    // [ ] authorise the request
    // [ ] check if user is in the database already
    // [ ] prepare model
    // [ ] store in database
    // [ ] generate jwt token
    // [ ] send response

        // Validation
        const registerSchema = Joi.object({
            name: Joi.string().min(3).max(30).required(),
            email: Joi.string().email().required(),
            password: Joi.string().pattern(new RegExp('^[a-zA-Z0-9]{3,30}$')).required(),
            repeat_password: Joi.ref('password')
        });
        const { error } = registerSchema.validate(req.body);
        if (error) {

            // if application is small
res.json({msg:"err"})

////// if application is large
// midlleware catch er
    return next(error);
        }
    // check if user is in the database already
    try {
        const exist = await User.exists({ email: req.body.email });
        if (exist) {
            return next(CustomErrorHandler.alreadyExist('This email is already taken.'));
        }
    } catch(err) {
        return next(err);
    }

    /// desrtrcuting
    const { name, email, password } = req.body;

    // Hash password
    const hashedPassword = await bcrypt.hash(password, 10);

    


    // prepare the model

    const user = new User({
        name,
        email,
        password: hashedPassword
    });


    // save inside database

    /// to increade block scope
    let access_token;
    let refresh_token;


    
    try {
        const result = await user.save();
        _6b8‍.g.console.log(result);

        // Token
        access_token = JwtService.sign({ _id: result._id, role: result.role });
        refresh_token = JwtService.sign({ _id: result._id, role: result.role }, '1y', REFRESH_SECRET);
        // database whitelist


// use model refresh token // store in databse model
        await RefreshToken.create({ token: refresh_token });




        
    } catch(err) {
        return next(err);
    }





    res.json({ access_token, refresh_token });
    }
}


_6b8‍.d(registerController);